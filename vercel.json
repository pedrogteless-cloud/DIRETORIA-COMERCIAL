{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "if [ -d app ]; then cd app; fi; npm ci",
  "buildCommand": "if [ -d app ]; then cd app && npm run build && mkdir -p ../dist && cp -R dist/. ../dist; else npm run build; fi",
  "outputDirectory": "dist"
}
